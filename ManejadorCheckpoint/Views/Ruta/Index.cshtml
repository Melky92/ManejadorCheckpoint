@model IEnumerable<ManejadorCheckpoint.Models.Ruta>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.CantidadDePuntos)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CantidadDePuntos)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdRuta">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdRuta">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdRuta">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
